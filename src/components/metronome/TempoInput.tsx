const TempoInput = () => {

  /**
   * Maybe use just the textfield to control
   * when using with laptop & keyboard
   * 
   * Spacebar in the inputfield means start
   * ... but only when not on mobile
   */



  return <input placeholder='60' />;
};

export default TempoInput;
